HASHES (WINDOWS)
================ 

7-Zip:
------
 
7-zip -> right click on the file + CRC SHA (CRC-32, CRC-64, SHA1, SHA256 and BLAKE2sp) 

or

7z h [-scrc{Method}] [files] 

Supported methods: CRC32, CRC64, SHA1, SHA256, BLAKE2sp. Default method is CRC32. 
(ex: 7z h -scrcSHA256 files) 
 
Certutil: 
---------

certutil -hashfile -? (Hash algorithms: MD2 MD4 MD5 SHA1 SHA256 SHA384 SHA512) 
(ex: certutil -hashfile file SHA256)

PowerShell Get-FileHash:
------------------------

Get-FileHash [-LiteralPath] <String[]> [[-Algorithm] <String>] [<CommonParameters>]
The acceptable values for this parameter are SHA1, SHA256, SHA384, SHA512 and MD5

More info: https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/get-filehash?view=powershell-7.1

Example:

Get-FileHash order.ps -Algorith MD5 | Format-List

Algorithm : MD5
Hash      : A25F7F0B29EE0B3968C860738533A4B9
Path      : C:\users\varanda\Desktop\order.ps

HASHES (LINUX)
============== 

7-Zip:
------
 
sudo apt install p7zip-full 
7z h [-scrc{Method}] [files] 
Supported methods: CRC32, CRC64, SHA1, SHA256, BLAKE2sp. Default method is CRC32. 
(ex: 7z h -scrcSHA256 files) 

Other tools: 
------------

{Method} [files] 
Supported methods: crc32, md5sum, sha1sum, sha256sum, sha512sum 
(ex:  sha256sum file) 


GPG 
========

gpg --full-generate-key # generate key pair
gpg --list-secret-keys --keyid-format=long # list keys

gpg --send-keys --keyserver hkp://keyserver.ubuntu.com 8D64DAC07C8B0BEA # export public key to server

gpg --export --armor name > /path/to/public-key.asc #export public key to a file
gpg --export-secret-keys --armor name > /path/to/secret-key-backup.asc #backup private key to a file
gpg —-import /path/to/secret-key-backup.asc #restore private key from a backup file

gpg --sign file        # produces file.gpg (signed file)
gpg --clear-sign file  # produces file.asc (clear text signed file)
gpg --detach-sign file # produces file.sig (detached signature file)

gpg --verify file.sig  # verify file.sig

gpg --keyserver keyserver.ubuntu.com --search-keys 8D64DAC07C8B0BEA      # Look for Keys / Import Keys from Server
gpg --keyserver keyserver.ubuntu.com --search-keys artur.varanda@sapo.pt # Look for Keys / Import Keys from Server
